# Do not commit real tokens, webhook URLs or email credentials.
alert_channels:
  telegram:
    enabled: true
    bot_token_secret_name: TELEGRAM_ALERT_BOT_TOKEN
    chat_id_secret_name: TELEGRAM_ALERT_CHAT_ID
  email:
    enabled: true
    smtp_host_secret_name: SMTP_HOST
    smtp_username_secret_name: SMTP_USERNAME
    smtp_password_secret_name: SMTP_PASSWORD
    to: ops@example.com
