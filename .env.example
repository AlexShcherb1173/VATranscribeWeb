APP_NAME=VATranscribe
APP_ENV=development
DEBUG=true
API_HOST=0.0.0.0
API_PORT=8000
API_PREFIX=/api/v1

COOKIES_DIR=./storage/cookies
YT_DLP_COOKIES_FILE=


# YouTube cookies are stored per-user encrypted in DB.
# Generate a Fernet key with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
YOUTUBE_COOKIES_ENCRYPTION_KEY=
YOUTUBE_COOKIES_MAX_BYTES=1048576
YOUTUBE_COOKIES_TEMP_DIR=./storage/tmp/ytdlp-cookies

# yt-dlp / YouTube
# YouTube client mode:
# Empty values = use yt-dlp default clients.
# If YT_DLP_YOUTUBE_PLAYER_CLIENT=mweb and YT_DLP_YOUTUBE_PO_TOKEN is empty,
# the app intentionally ignores mweb and falls back to yt-dlp default clients.
# Use mweb only together with a valid PO token:
# YT_DLP_YOUTUBE_PLAYER_CLIENT=mweb
# YT_DLP_YOUTUBE_PO_TOKEN=mweb.gvs+PASTE_TOKEN_HERE
YT_DLP_PROXY_URL=
YT_DLP_YOUTUBE_PLAYER_CLIENT=
YT_DLP_YOUTUBE_PO_TOKEN=

PYTHONPATH=.

DATABASE_URL=
REDIS_URL=

STORAGE_ROOT=./storage
UPLOADS_DIR=./storage/uploads
DOWNLOADS_DIR=storage/downloads
DOWNLOADS_AUDIO_DIR=./storage/downloads/audio
DOWNLOADS_VIDEO_DIR=./storage/downloads/video
TRANSCRIPTS_TXT_DIR=./storage/transcripts/txt
TRANSCRIPTS_SRT_DIR=./storage/transcripts/srt
TRANSCRIPTS_VTT_DIR=./storage/transcripts/vtt
TRANSCRIPTS_JSON_DIR=./storage/transcripts/json
TEMP_DIR=./storage/temp
LOGS_DIR=./storage/logs

FFMPEG_PATH=/usr/bin/ffmpeg
FFPROBE_PATH=/usr/bin/ffprobe
YT_DLP_PATH=yt-dlp

DEFAULT_LANGUAGE=ru
DEFAULT_TRANSCRIPTION_MODEL=medium

SECRET_KEY=super-secret-key-change-me
ACCESS_TOKEN_EXPIRE_MINUTES=1440

CORS_ORIGINS=http://localhost:5175,http://127.0.0.1:5175

VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
# Marketing -> SaaS app links
PUBLIC_VATRANSCRIBE_APP_URL=http://127.0.0.1:5175

# Marketing public URL used for canonical URLs, sitemap, robots.txt and JSON-LD
PUBLIC_VATRANSCRIBE_MARKETING_URL=http://localhost:4321

WEB_HTTP_PORT=8080

VITE_BASE_PATH=/app/
EXPOSE_API_DOCS=true
PUBLIC_MARKETING_ORIGIN=http://localhost:4321
PUBLIC_APP_ORIGIN=http://localhost:5175
PUBLIC_API_ORIGIN=http://127.0.0.1:8000
PUBLIC_ADMIN_ORIGIN=http://localhost:5175
JWT_ALGORITHM=HS256
REFRESH_TOKEN_EXPIRE_DAYS=30
COOKIE_SECURE=false
COOKIE_HTTPONLY=true
COOKIE_SAMESITE=lax
COOKIE_DOMAIN=

REFRESH_COOKIE_NAME=vatranscribe_refresh_token
CSRF_COOKIE_NAME=vatranscribe_csrf_token
CSRF_HEADER_NAME=X-CSRF-Token
